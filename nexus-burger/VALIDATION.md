# 连续滚动与圆桌入席验收

日期：2026-09-22—23。Windows、Node.js 25.2.1、独立无头 Chromium。检查生产构建的 `http://127.0.0.1:4173/burger/`，未操作用户日常浏览器。

## 本轮结果

- 每个方向的三维配方物件排列在连续长架上。原生纵向滚动驱动整排横向移动，两端裁剪；取消按组翻页。保留原始 107 个地址及六个分类。
- 左侧目录固定显示原有方向、伙伴院校与入席，持续高亮当前章节；点击直接定位。删除原先短暂出现的小标题视觉展示，保留语义标题。
- 缓存文档布局，减少动画帧内的布局读取；只挂载当前方向，架外物件不绘制。动画使用阻尼过渡，阴影最多每秒更新 24 次，像素比上限 1.25。保留原生滚轮与纵向触摸。
- 入席时各食材沿随机路径飞来、弹性叠合，旋转三圈并伴随柔和光束；落桌后微微发光。同一次入席倒滚可还原路径，再次进入重新随机。
- 修正腾空阶段镜头裁切、旧环境残影及入席标题经过目录的遮挡。
- 46 位成员仍各有自己的校徽桌牌。肩、肘、腕使用连续关节位置，增加长短发、眼镜、帽子、卫衣、西装及夹克，普渡成员为长发。桌面持续 5 人、手机与矮横屏持续 4 人发言，从 144 条技术与生活话语中错峰轮换。
- 全站中文采用站酷快乐体本地子集（Nexus Round，约 97 KiB），英文采用 Lilita One；目录、标题、气泡、名录和 3D 纸签统一，纸签字体就绪后重新绘制，公式保留等宽字体。各层特写缩小 8%，保留金色粒子、光束与光晕。别称说明和“我们热爱汉堡，正如我们热爱大语言模型！”仅在页脚显示。
- 对照在线主站同步港科广 8 人、南科大 3 人，总计仍为 46 人；逐校人数加入主站对照校验。144 条讨论全部改为口语化的技术提问、调试吐槽和生活接话；气泡按投影桌面边界向外围布局，避让汉堡、目录、标题及透明邀请席。
- 保留圆桌慢转、拖动与方向键旋转、白色透明邀请席及红色欢迎引线；保留激情印章、独立院校转场与大面积红色收尾。

## 自动检查

`npm test`：**11 passed**。覆盖原始链接与分类、公开院校与校徽、时间轴连续性和可逆性、层次顺序、46 个成员座位、文档坐标映射、随机路径最终精确归位、食材几何有效性，以及技术语料的覆盖范围、唯一性和不重复抽样。

`npm run build`：TypeScript 检查及 Vite 生产构建通过，产物写入 `../burger/`。

10 项浏览器检查均通过。本次完整运行 9 项通过，1 项因旧本地预览服务断开报 `ERR_CONNECTION_REFUSED`；重启服务后单独重测该导航／减少动态效果用例通过。完整运行记录位于 `artifacts/full-browser-run.json`，导航重测记录为 `artifacts/navigation-recheck.json`。最后微调小屏气泡宽度和邀请席避让区域后，四种视口的对话轮换／边界／互不重叠检查再次通过，报告为 `test-results/qa-results.json`。

1. 107 个链接与原始目的地址一致；三维物件可直接点击；无弹窗、抽屉或展开控件。
2. 桌面六种环境和食材特写、倒滚还原、其余食材的垂直关系、稳定目录。
3. 1920 × 1080 下的物件占比、46 位成员与逐人校徽、白色邀请席，欢迎引线投影误差小于 4px；1366 × 768 与 844 × 390 下入席标题不与目录相交。
4. 390 × 844 下全部六个分类、物件点击区域、名录、邀请标注及无横向溢出。
5. 圆桌自动旋转、拖动、松手与原生纵向滚动。
6. 逐段遍历长架，确认全部 107 个三维物件可达；点击区域与投影一致；键盘可聚焦架外物件并自动定位。
7. 原生图标导航和减少动态效果模式；关闭自动旋转、飞行动画与气泡后仍能阅读和手动旋转。
8. 小幅滚轮使物件位置连续递增，目录位置不跳动；点击目录切换场景；验证飞行位移、随机路径更新、三圈旋转、人物多样性、普渡长发及气泡出现。
9. WebGL 不可用时保留海报、完整文字资源和公开院校名录。
10. 四种视口中持续 4～5 个技术气泡，成员不重复；气泡在屏幕内且彼此不相交；连续采样期间人数不减少，内容自动更新。另检查目录字体实际加载及页脚文案唯一。

外链跳转测试拦截目标响应，只验证链接行为；不代表逐一检查第三方站点当前可用性。

## 视觉复核

`npm run capture` 覆盖 1440 × 900、1920 × 1080、1366 × 768、820 × 1180、390 × 844、844 × 390，共 28 张截图。包括六种环境、首屏、圆桌、院校、分隔和红色结尾。

最终气泡布局另复核这六种尺寸的圆桌截图，记录为 `artifacts/table-layout-review.json`。

此前 `node scripts/capture-motion.mjs` 另存 9 张运动截图：连续长架的起点、中间偏移与终点，以及飞入、叠合、旋转、落桌、入席。记录在 `artifacts/motion-review.json`。本次未改动这些运动路径。

截图采集记录为 `artifacts/browser-errors.json`；本次最终 28 张截图采集无脚本、着色器或资源请求错误。采集脚本现在遇到错误会以失败退出。截图和测试报告不进入发布目录。软件渲染浏览器中的流程检查不能替代所有真实设备的帧率测试。

## 保留范围

原站 `index.html`、`style.css`、`script.js`、`assets/`、`ecommerce-agent/` 的定向 `git diff` 为空。发布目标为 `https://ryannnice.github.io/nexus/burger/`，沿用 main 分支的 Pages 流程。原始 `聊天记录.md` 已加入本地忽略规则，不进入版本库及构建产物。

食材及道具均为实时程序化三维网格，并非扫描资产；名称使用三维纸签，普通链接根据物体投影定位。
