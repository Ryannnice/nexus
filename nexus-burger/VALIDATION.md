# 场景重构验收记录

日期：2026-09-22。Windows、Node.js 25.2.1、独立无头 Chromium 153。验收使用生产构建的 `/burger/` 嵌套路径，未连接用户日常浏览器。

## 本轮完成

- 学习资料改为小物件链接：厨具、调料罐、种植袋和蛋盒。保留全部 107 个原始地址和六个原分类。完整名称保留在可访问名称与悬停提示中；点击直接跳转，没有展开页或抽屉。
- 1920 横屏中，资料区宽度小于视口的 25%、面积小于视口的 14%。主体空间留给食材与背景。
- 首屏汉堡放大，食材展示改为跟随各层的镜头特写，当前层前移并放大约 30%。其他层保留原有大小和垂直关系，在特写画面边缘露出。
- 加入六个三维背景：烤箱、烘焙台、芝士柜、农场、铁板调料台、蛋盒工作台。背景随滚动章节淡入淡出。
- 圆桌、汉堡、46 位成员与 46 个校徽桌牌作为整体缓慢自转。鼠标横向拖动或左右方向键可旋转，纵向滚动保留原生行为。
- 邀请小人改为白色半透明物理材质，移除红色线框。红色欢迎标注跟随旋转后的三维位置，转至桌后时淡出，文字位置限制在视口内。
- 保留 BURGER KING 标题与 NEXUS 别称注解，以“一起入席，成为新的汉堡王！”收尾。

## 自动检查

TypeScript 检查与 Vite 生产构建通过。

**8 项核心检查通过**：原始资源及分类、公开院校与本地校徽、场景端点、时间轴连续性与可逆性、分层顺序与放大范围、实际文档位置映射、46 个成员席位与独立邀请席位、食材实体几何。

**7 项浏览器流程通过**：

1. 107 个原始资料链接均可直接访问；标题与昵称注解正确；无弹窗、筛选、输入或展开控件。
2. 桌面依次进入六种特写和背景，并倒滚返回。根据真实网格顶点投影检查主体大小与位置，确认其余食材未删除或移出原有垂直轴线。
3. 1920 × 1080 资料区占比；46 个人物实例、46 个校徽及其逐位院校对应关系；白色邀请席位；红线端点与三维头部的屏幕投影误差小于 4px。
4. 390 × 844 全部六个分类，检查主体、底部小物件链接、名录、欢迎标注及无横向溢出。
5. 圆桌角度自动递增；拖动改变角度；松开后正确退出拖动态；鼠标滚轮仍使页面纵向移动。
6. 图标导航与系统减少动态效果；减少动态效果时停止自转，保留方向键手动旋转；返回首页状态正确。
7. 禁用 WebGL 后保留静态海报、完整资源与成员名录，以及原站入口。

结果：`7 passed`，报告位于 `test-results/qa-results.json`。

## 视觉复核

21 张截图覆盖 1440 × 900、1920 × 1080、820 × 1180、390 × 844、844 × 390。桌面额外保存农场、铁板和蛋盒场景。主要复核主体占比、六种背景、小物件链接、白色席位与旋转中的标注。

截图及错误记录在 `artifacts/`，不进入发布目录。截图采集没有脚本错误或资源请求失败。

原站所有已跟踪文件的 `git diff` 为空，旧主页与电商 Agent 保持原样。没有提交、推送或线上发布。预览：`http://127.0.0.1:4173/burger/`。

场景和食材均为程序化实时模型，未使用食物扫描资产。资料物件为带有标题的轻量 SVG 链接，背景橱窗和道具为三维网格。浏览器检查不代表所有真实设备的帧率；未逐一测试第三方资源网站的在线可用性。
